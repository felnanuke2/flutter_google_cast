/// Enum representing different types of media tracks.
enum TrackType {
  /// Unknown track type.
  unknown,

  /// Text track (e.g., subtitles, captions).
  text,

  /// Audio track.
  audio,

  /// Video track.
  video,
}
